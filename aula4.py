numeros=[33,11,22,54,51,52,17,33]
nomes=['julia', 'maria', 'luiza', 'enzo']

maior= max(numeros)
menor=min(numeros)
print(f"o maior numero é:{maior}, e o menor é:{menor}")


#sort() organiza os elementos de uma lista em ordem crescente. No caso da lista 'numeros', após a execução do método sort(), os elementos serão rearranjados do menor para o maior valor.
#reverse() inverte a ordem dos elementos de uma lista. Se aplicada à lista 'numeros' após o sort(), os elementos serão organizados do maior para o menor valor.
#len() retorna o número de elementos em uma lista. Por exemplo, len(numeros) retornará 7, pois há 7 elementos na lista 'numeros'.
#count() conta quantas vezes um determinado elemento aparece em uma lista. Por exemplo, numeros.count(33) retornará 2, pois o número 33 aparece duas vezes na lista 'numeros'.
#sum() calcula a soma de todos os elementos em uma lista. Por exemplo, sum(numeros) retornará 290, que é a soma de todos os números na lista 'numeros'.